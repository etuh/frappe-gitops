# Template only. Do not apply as-is.
# Generate a real SealedSecret from base/secrets.example.yaml and store it in your overlay.
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: frappe-secrets
spec:
  encryptedData:
    db-password: REPLACE_WITH_SEALED_VALUE
    admin-password: REPLACE_WITH_SEALED_VALUE
  template:
    metadata:
      name: frappe-secrets
    type: Opaque
