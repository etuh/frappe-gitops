# Replace these placeholder values with real sealed values generated for your cluster.
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: frappe-secrets
spec:
  encryptedData:
    db-password: REPLACE_WITH_SEALED_VALUE
    admin-password: REPLACE_WITH_SEALED_VALUE
  template:
    metadata:
      name: frappe-secrets
    type: Opaque
